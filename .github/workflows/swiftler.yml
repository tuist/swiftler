name: Swiftler

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  formatting:
    name: Check Formatting
    strategy:
      matrix:
        os: [namespace-profile-default-macos, namespace-profile-default]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Swift on Linux
        if: matrix.os == 'namespace-profile-default'
        run: |
          # Install Swift 6.0.2 on Linux
          wget https://download.swift.org/swift-6.0.2-release/ubuntu2204/swift-6.0.2-RELEASE/swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          tar xzf swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          sudo mv swift-6.0.2-RELEASE-ubuntu22.04 /usr/share/swift
          echo "/usr/share/swift/usr/bin" >> $GITHUB_PATH
          # Test that Swift is working
          /usr/share/swift/usr/bin/swift --version

      - name: Set up Mise
        uses: jdx/mise-action@v2

      - name: Install dependencies with Mise
        run: mise install

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Check formatting
        run: mix format --check-formatted

  compile:
    name: Compile
    strategy:
      matrix:
        os: [namespace-profile-default-macos, namespace-profile-default]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: List available Xcode versions
        if: matrix.os == 'namespace-profile-default-macos'
        run: ls -la /Applications/ | grep Xcode || true

      - name: Select Xcode 16
        if: matrix.os == 'namespace-profile-default-macos'
        run: |
          # Try different possible Xcode 16 paths
          if [ -d "/Applications/Xcode_16.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.0.app
          elif [ -d "/Applications/Xcode_16_beta.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16_beta.app
          elif [ -d "/Applications/Xcode-16.0.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode-16.0.0.app
          else
            echo "Warning: Xcode 16 not found, using default"
          fi
          swift --version

      - name: Set up Swift on Linux
        if: matrix.os == 'namespace-profile-default'
        run: |
          # Install Swift 6.0.2 on Linux
          wget https://download.swift.org/swift-6.0.2-release/ubuntu2204/swift-6.0.2-RELEASE/swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          tar xzf swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          sudo mv swift-6.0.2-RELEASE-ubuntu22.04 /usr/share/swift
          echo "/usr/share/swift/usr/bin" >> $GITHUB_PATH
          # Test that Swift is working
          /usr/share/swift/usr/bin/swift --version

      - name: Set up Mise
        uses: jdx/mise-action@v2

      - name: Install dependencies with Mise
        run: mise install

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile Elixir
        run: mix compile --warnings-as-errors

      - name: Build Swift package
        run: swift build -Xswiftc -warnings-as-errors
        working-directory: .

  test:
    name: Run Tests
    strategy:
      matrix:
        os: [namespace-profile-default-macos, namespace-profile-default]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: List available Xcode versions
        if: matrix.os == 'namespace-profile-default-macos'
        run: ls -la /Applications/ | grep Xcode || true

      - name: Select Xcode 16
        if: matrix.os == 'namespace-profile-default-macos'
        run: |
          # Try different possible Xcode 16 paths
          if [ -d "/Applications/Xcode_16.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.0.app
          elif [ -d "/Applications/Xcode_16_beta.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16_beta.app
          elif [ -d "/Applications/Xcode-16.0.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode-16.0.0.app
          else
            echo "Warning: Xcode 16 not found, using default"
          fi
          swift --version

      - name: Set up Swift on Linux
        if: matrix.os == 'namespace-profile-default'
        run: |
          # Install Swift 6.0.2 on Linux
          wget https://download.swift.org/swift-6.0.2-release/ubuntu2204/swift-6.0.2-RELEASE/swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          tar xzf swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          sudo mv swift-6.0.2-RELEASE-ubuntu22.04 /usr/share/swift
          echo "/usr/share/swift/usr/bin" >> $GITHUB_PATH
          # Test that Swift is working
          /usr/share/swift/usr/bin/swift --version

      - name: Set up Mise
        uses: jdx/mise-action@v2

      - name: Install dependencies with Mise
        run: mise install

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Run Elixir tests
        run: mix test

  example:
    name: Test Example
    strategy:
      matrix:
        os: [namespace-profile-default-macos, namespace-profile-default]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: List available Xcode versions
        if: matrix.os == 'namespace-profile-default-macos'
        run: ls -la /Applications/ | grep Xcode || true

      - name: Select Xcode 16
        if: matrix.os == 'namespace-profile-default-macos'
        run: |
          # Try different possible Xcode 16 paths
          if [ -d "/Applications/Xcode_16.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16.0.app
          elif [ -d "/Applications/Xcode_16_beta.app" ]; then
            sudo xcode-select -s /Applications/Xcode_16_beta.app
          elif [ -d "/Applications/Xcode-16.0.0.app" ]; then
            sudo xcode-select -s /Applications/Xcode-16.0.0.app
          else
            echo "Warning: Xcode 16 not found, using default"
          fi
          swift --version

      - name: Set up Swift on Linux
        if: matrix.os == 'namespace-profile-default'
        run: |
          # Install Swift 6.0.2 on Linux
          wget https://download.swift.org/swift-6.0.2-release/ubuntu2204/swift-6.0.2-RELEASE/swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          tar xzf swift-6.0.2-RELEASE-ubuntu22.04.tar.gz
          sudo mv swift-6.0.2-RELEASE-ubuntu22.04 /usr/share/swift
          echo "/usr/share/swift/usr/bin" >> $GITHUB_PATH
          # Test that Swift is working
          /usr/share/swift/usr/bin/swift --version

      - name: Set up Mise
        uses: jdx/mise-action@v2

      - name: Install dependencies with Mise
        run: mise install

      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: |
            deps
            example/calculator/deps
          key: ${{ runner.os }}-example-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-example-

      - name: Install Swiftler dependencies
        run: mix deps.get

      - name: Install example dependencies
        run: mix deps.get
        working-directory: example/calculator

      - name: Compile example
        run: mix compile
        working-directory: example/calculator

      - name: Run example tests
        run: mix test
        working-directory: example/calculator